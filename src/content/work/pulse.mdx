---
title: Pulse offline-sync engine
summary: The offline-first mobile engine behind an enterprise inspection platform that grew to 100+ B2B clients. No data-loss incidents reported in 4.5 years of field use.
role: Head of Mobility / Lead Mobile Engineer
period: Jan 2021 – May 2025
client: Aim North Technologies (Pulse Business Solutions)
platform: Android and iOS, native then React Native
stack: [Kotlin, Swift, React Native, SQLite, GitHub Actions, Fastlane]
order: 2
---

## The job

I owned mobile for the Pulse enterprise inspection platform, Android and iOS end to end. I architected the mobile engine from an early prototype to **100+ B2B enterprise clients**.

## A sync contract first

I designed the sync contract with the backend and web teams: payloads, upload
semantics and failure behaviour. Then I implemented both native clients against it.

## Built for auditors without signal

Auditors inspect sites with no connectivity, so the data layer is offline-first:

- a **dynamic form builder** for inspection forms
- a **background media upload queue**
- a **local SQLite cache**

**No data-loss incidents were reported in 4.5 years of field use.**

## Two native clients, kept in parity

I wrote and shipped the Swift/iOS client to the App Store alongside Android, with feature parity between
the two. The field UIs are usable by non-technical staff with no training.

## One codebase: the React Native rewrite

In 2025 I led a full rewrite of both native apps into a single **React Native** codebase. I architected it
and built it alongside the team, and we completed it in six months, before I left in May 2025.

The reason was maintenance and speed: with two native apps, every feature was built twice, and one codebase
lets features and fixes reach Android and iOS at once. The native apps' behaviour was replicated in React
Native rather than redesigned. I built the core myself (sync, offline storage and uploads), and the team
built the screens and features on top. The new app rolled out gradually, with the native app still
available while users moved over.

## Releases that stopped being events

I introduced the release process with GitHub Actions and Fastlane: a predictable bi-weekly cadence and
same-day hotfixes, replacing manual builds.
